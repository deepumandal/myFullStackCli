export const AddCodeQualityCMD = async () => {
  console.log("Adding code quality tools...");
  console.log("Installing ESLint, Prettier, commitlint and Husky...");

  await Promise.resolve();
  //   await backupAndCleanOldConfigs();

  //   for (const command of CodeQualityDeps()) {
  //     console.log(`Executing command: ${command}`);
  //     await executeCommands(command);
  //   }
};
